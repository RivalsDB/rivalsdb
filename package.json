{
  "name": "rivalsdb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build-server && npm run build-client && npm run build-assemble",
    "build-client": "cd client && npm install && npm run build && cd ..",
    "build-server": "cd server && npm install && npm run build && cd ..",
    "build-assemble": "mv client/public/* server/public/",
    "start": "node server/dist/index.js"
  },
  "author": "Luiz Socrate <luiz@socrate.com.br>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lsocrate/rivalsdb/issues"
  },
  "homepage": "https://rivalsdb.app",
  "engines": {
    "node": "16.8.0"
  }
}
